

export function ReportsAnalyticsPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>
        Reports & Analytics
      </h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Track trends, performance reports, and financial summaries.
      </p>
    </div>
  );
}

export function ActivityLogPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Activity History</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Review recent admin actions, updates, and system events.
      </p>
    </div>
  );
}

export function SalesPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Sales / Income</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Manage income records, transactions, and payment summaries.
      </p>
    </div>
  );
}

export function ExpensesPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Expenses</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Review outgoing costs, bills, and expense claims.
      </p>
    </div>
  );
}

export function ProductsPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Products / Stock</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Track inventory, stock levels, and product management.
      </p>
    </div>
  );
}

export function BranchesPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Branches</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Manage branch locations, details, and status updates.
      </p>
    </div>
  );
}

export function ManageWorkersPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Manage Workers</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Add, edit, and oversee assigned workers and roles.
      </p>
    </div>
  );
}

export function WorkerLinksPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>
        Worker Invite Links
      </h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Create and manage invitation links for workers.
      </p>
    </div>
  );
}

export function NotificationsPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Notifications</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        View alerts, updates, and important notices.
      </p>
    </div>
  );
}

export function SettingsPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Settings</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Adjust system preferences and administrator controls.
      </p>
    </div>
  );
}

export function HelpCenterPage() {
  return (
    <div
      style={{
        padding: "24px",
        background: "#fff",
        borderRadius: "16px",
        boxShadow: "0 10px 30px rgba(15, 23, 42, 0.06)",
      }}
    >
      <h2 style={{ margin: "0 0 8px", color: "#0f172a" }}>Help Center</h2>
      <p style={{ margin: 0, color: "#64748b" }}>
        Find support guides, FAQs, and admin resources.
      </p>
    </div>
  );
}
